GravewalkerAura:
  Skills:
  - effect:particles{particle=ash;amount=2;vSpread=1;hSpread=0.5} @self
  
  # CORRECTED: 'cleartarget' works on standard Vanilla AI, forcing them to drop aggro
  - cleartarget @MobsInRadius{types=ZOMBIE,SKELETON,HUSK,DROWNED,STRAY,WITHER_SKELETON;r=15}